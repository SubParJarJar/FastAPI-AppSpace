name: Trigger auto deployment for container-app-an6i6meghcg46

# When this action will be executed
on:
  # Automatically trigger it when detected changes in repo
  push:
    branches: 
      [ main ]
    paths:
    - '//**'
    - '.github/workflows/container-app-an6i6meghcg46-AutoDeployTrigger-96df66bd-5c8f-4160-9048-7548ba5b4772.yml'

  # Allow manual trigger 
  workflow_dispatch:      

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest
    permissions: 
      id-token: write #This is required for requesting the OIDC JWT Token
      contents: read #Required when GH token is used to authenticate with private repo

    steps:
      - name: Checkout to the branch
        uses: actions/checkout@v2

      - name: Azure Login
        uses: azure/login@v1
        with:
          client-id: ${{ secrets.CONTAINERAPPAN6I6MEGHCG46_AZURE_CLIENT_ID }}
          tenant-id: ${{ secrets.CONTAINERAPPAN6I6MEGHCG46_AZURE_TENANT_ID }}
          subscription-id: ${{ secrets.CONTAINERAPPAN6I6MEGHCG46_AZURE_SUBSCRIPTION_ID }}

      - name: Build and push container image to registry
        uses: azure/container-apps-deploy-action@v2
        with:
          appSourcePath: ${{ github.workspace }} 
          registryUrl: 
          registryUsername: ${{ secrets.CONTAINERAPPAN6I6MEGHCG46_REGISTRY_USERNAME }}
          registryPassword: ${{ secrets.CONTAINERAPPAN6I6MEGHCG46_REGISTRY_PASSWORD }}
          containerAppName: container-app-an6i6meghcg46
          resourceGroup: fastapiappspace87bb
          imageToBuild: default/[parameters('containerAppName')]:${{ github.sha }}
          _buildArgumentsKey_: |
            _buildArgumentsValues_


